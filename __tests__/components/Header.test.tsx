import { expect, test } from 'vitest';
import Header from '../../src/components/Header';
import { render, screen } from '../test-utils';

test('Header', () => {
  expect(true);
  //   render(<Header errorAuth={null} isAuth={null} />);
  //   expect(screen.getByRole('link', { name: 'Home' })).toBeDefined();
});
